# PXAPI Decision Ledger

This ledger records delivery-relevant accepted decisions and their authority. It is an index, not a replacement for the cited authority.

| ID | Status | Decision | Authority | Evidence boundary |
| --- | --- | --- | --- | --- |
| D-PXAPI-001 | ACCEPTED | Federated authority by claim type; no global SSoT. | PXAPI project governance / Confluence | Requires live source read for current claims. |
| D-PXAPI-002 | ACCEPTED | One canonical Analysis Run / Analysis Truth feeds internal and external projections. | PXAPI Confluence `39846055`, amendment `52527106` | Target/product decision; does not prove runtime implementation. |
| D-PXAPI-003 | ACCEPTED | Measurement, Business Context, Synthesis, Scoring/Product Diagnosis, Customer Projection, Rendering and adapters are separate responsibilities. | PXAPI Confluence `39846055` | Architecture intent, not runtime proof. |
| D-PXAPI-004 | ACCEPTED | Missing, unavailable, provider-error or not-assessed evidence must not become website weakness or score penalty. | PXAPI Confluence `39846055`, `52527106`; Skill baseline | Cross-cutting semantic invariant. |
| D-PXAPI-005 | ACCEPTED | Skill parity is source-bound to an exact frozen Skill snapshot; no floating `latest skill` target. | PXAPI Confluence `52527106`; PXK-web-skill docs | Each new parity increment needs a fresh frozen reference. |
| D-PXAPI-006 | ACCEPTED | Acquisition and Site Intelligence belong to PXAPI; the Skill remains migration source / Regression Oracle. | PXAPI Confluence `54362115` | Target architecture. |
| D-PXAPI-007 | ACCEPTED | Sampling is Pixelkiez methodology; providers must not decide score-/diagnosis-relevant pages. | PXAPI Confluence `54362115` | Target architecture. |
| D-PXAPI-008 | ACCEPTED | Census first; deterministic stratified sampling only above a benchmark-based threshold. | PXAPI Confluence `54362115` | Exact threshold remains MISSING until benchmarked/versioned. |
| D-PXAPI-009 | ACCEPTED | Static HTTP and rendered-browser observations are separate modes; delta is not automatically conflict. | PXAPI Confluence `54362115` | Target evidence model. |
| D-PXAPI-010 | ACCEPTED | Crawl4AI may be used behind a provider-neutral adapter; it is not domain authority. | PXAPI Confluence `54362115` | Provider version/security/license must be reverified at implementation time. |
| D-PXAPI-011 | ACCEPTED | API and worker roles remain one Python codebase/modular monolith initially; no premature microservice split. | PXAPI Confluence `54362115` | Runtime isolation may still use separate process/container. |
| D-PXAPI-012 | ACCEPTED | Business action hypotheses and later impact verification are separate artifacts. | PXAPI Confluence `54362115` | No causal/ROI proof follows from diagnosis alone. |
| D-PXAPI-013 | ACCEPTED | WIP limit is one mutating implementation theme. | PXAPI governance / delivery policy | Planning/discovery can proceed read-only. |
| D-PXAPI-014 | ACCEPTED | Delivery gates follow IC -> R2G -> R4M; exact-candidate binding is mandatory. | PXAPI DRS / anti-drift governance | Candidate change invalidates R2G/R4M. |
| D-PXAPI-015 | ACCEPTED | Merge != Done; CI != runtime; agent report != independent verification. | PXAPI anti-drift governance | Closeout needs source-specific readback. |
| D-PXAPI-016 | ACCEPTED | Pareto-first acquisition path is PXAPI-4 -> PXAPI-19 -> PXAPI-20 -> measure evidence gain before browser complexity. | Product Owner decision recorded in PXAPI architecture/Jira receipt | Value expectation is a hypothesis until measured. |

| D-PXAPI19-PO-001 | ACCEPTED | `/robots.txt` support in PXAPI-19 is bounded: fetch it same-origin and parse only `Sitemap:` declarations as additional sitemap seeds. No `Allow`/`Disallow`/`User-agent`/`Crawl-delay` semantics and no robots-compliance claim; failures stay neutral. | Product Owner decision, Jira `PXAPI-19` comment 2026-09-10; Confluence `54820865` | Scope decision for the discovery slice. It authorises no crawler policy of any kind. |
| D-PXAPI19-PO-002 | ACCEPTED | `selection_complete` is kept. `CENSUS` with `selection_complete = false` means a runtime/safety budget prevented complete selection, and must never be read as complete coverage, a website defect or a score penalty. | Product Owner decision, Jira `PXAPI-19` comment 2026-09-10; Confluence `54820865` | Semantic invariant. Neutrality is enforced in the producer and the contract, not by this row. |
| D-PXAPI19-PO-003 | ACCEPTED | PXAPI-19 is delivered as 19.A then 19.B under one Jira item and one WIP theme: 19.A is contracts/foundation only, 19.B the producer/discovery/runtime slice, with at most one active mutating PXAPI-19 PR at a time. | Product Owner decision, Jira `PXAPI-19` comment 2026-09-10; Confluence `54820865` | Delivery sequencing. A successful 19.A candidate does not satisfy the whole ticket. |
| D-PXAPI19-PO-004 | ACCEPTED | Digest topology: `site-inventory.v1` and `sampling-manifest.v1` each carry exactly `input_digest` and `output_digest`; no third `content_digest`. The manifest may bind `inventory_output_digest`. Digests exclude envelope IDs/timestamps and are order-independent where semantically required. | Product Owner decision, Jira `PXAPI-19` comment 2026-09-10; Confluence `55050241` | Contract decision. The final clause holds for each document's **own** envelope ids and timestamps. The manifest's `inventory_ref` is not a manifest envelope member but a digest-relevant upstream input of `sampling-manifest.input_digest`; that reading is `D-PXAPI19-PO-012`, which resolved `C-PXAPI-010` by documentation authority on 2026-09-19. **Prior wording of this cell, preserved:** "Contract decision. The final clause is in tension with the shipped `MANIFEST_INPUT`, which includes `inventory_ref` — see `C-PXAPI-010`; that tension is unresolved and is not resolved by this index row." |
| D-PXAPI19-PO-005 | ACCEPTED | One `sources[]` model carries source state; it is not duplicated by root `sitemap_state` / `robots_state` members. Source outcomes remain neutral process evidence. | Product Owner decision, Jira `PXAPI-19` comment 2026-09-10; Confluence `55050241` | Contract decision. Cited by `src/pxapi/domain/site_discovery.py`. |
| D-PXAPI19-PO-006 | ACCEPTED | Bootstrap failure produces no inventory and no manifest: an empty or fabricated `SiteInventory` is invalid, an emitted inventory carries at least the canonical target seed, and the failure creates no website finding and no score penalty. | Product Owner decision, Jira `PXAPI-19` comment 2026-09-10; Confluence `55050241` | Semantic invariant. Enforced by the producer and the contract. |
| D-PXAPI19-PO-007 | ACCEPTED | Canonical duplicate URLs aggregate provenance rather than becoming an exclusion; no `DUPLICATE_URL_KEY` without separate justification; arbitrary rejected attacker-controlled URL strings are not persisted for digest convenience; raw anchor labels stay transient and outside the 19.A contract. | Product Owner decision, Jira `PXAPI-19` comment 2026-09-10; Confluence `55050241` | Contract decision. Cited by `src/pxapi/domain/site_discovery.py` and the acquisition semantics. |
| D-PXAPI19-PO-008 | ACCEPTED | The historical exact-head `R4M` for PR #16 (PXAPI-19.B, candidate `7e5d67e`, merge `fe8d838`) is `UNKNOWN — HISTORICAL GOVERNANCE EXCEPTION`, permanently. It is never reconstructed, inferred or recorded as `GREEN`, and no retrospective approval is manufactured. | Product Owner decision, Jira `PXAPI-19` comment `16044` (2026-09-17) | Governs PR #16 only. A later candidate's own gate — including PR #18's `R4M` in `D-PXAPI19-PO-010` — neither alters nor substitutes for it. See `C-PXAPI-008`, AD-005. |
| D-PXAPI19-PO-009 | ACCEPTED | Discovery-correctness repair finding dispositions: `C-PXAPI-013` closed on the candidate, together with the sibling `_robots()` defect NF-5; `C-PXAPI-014` closed as the implementation contradiction; `C-PXAPI-015` superseded as an implementation defect, because the current contract defines `CANONICAL_SEED` as the origin the bootstrap established and requires an admitting outcome for it; `C-PXAPI-016` non-material as an implementation defect under the currently authoritative named-limit definitions. | Orchestrator / Product Owner decision `AD-008`, Jira `PXAPI-19` comment `16081` (2026-09-18) | Supersedes the implementation-defect reading of findings 2–4 of comment `16045` only where the exact-candidate evidence shows a narrower truth. Authorises no code or contract change for `C-PXAPI-015`; a non-2xx seed state remains a contract question (NF-1 in `docs/evidence/PXAPI-19B-discovery-correctness-repair.md`). The `C-PXAPI-016` verdict must be re-measured if an authority restates what a named limit bounds. |
| D-PXAPI19-PO-010 | ACCEPTED | PR #18 (discovery-correctness repair) is `IC = GREEN`, `R2G = GREEN`, `R4M = GREEN — READY FOR MERGE` at exact head `95c061a0716a25031145587a3cc40bbf5c04b6a4` on base `aa75d6b52fe060b76a7cf900617ad5f990901183`; the `R4M` was consumed by merge `bfe06cc07ea0dfde31fedb8e7115940da24fb9fd`. | IC and R2G `GREEN` first recorded in Jira `PXAPI-19` comment `16081` (`AD-008`) and restated, with R4M `GREEN`, in Orchestrator gate `AD-009`, comment `16082` (2026-09-18); merge receipt comment `16083` | Applies to that exact head only. It is not a ticket-level gate, not Jira Done, and does not touch `D-PXAPI19-PO-008`. |
| D-PXAPI19-PO-011 | ACCEPTED | PXAPI-19 AC8 is accepted for ticket closeout on a **fresh** Real-Boundary reproduction: attached clean `main` checkout at `bfe06cc07ea0dfde31fedb8e7115940da24fb9fd`, target `https://www.rfc-editor.org/`, Python 3.13.3 and 3.14.6, opt-in smoke passed on both. | Product Owner closeout disposition, 2026-09-19, issued as the binding brief for the PXAPI-19 final closeout PR; **not yet recorded in Jira** — Jira `PXAPI-19` readback is a precondition for `PXAPI-20` | Accepted only with its evidence ceiling: one controlled public-origin Discovery → SiteInventory → SamplingManifest boundary. Not complete site coverage, arbitrary crawling, production scale, browser rendering, PXAPI-20, scoring quality, customer output or customer value. A new reproduction, never a retroactive upgrade of the historical 2026-09-11 record. Receipt: `docs/evidence/PXAPI-19-AC8-real-boundary-receipt.json`. |
| D-PXAPI19-PO-012 | ACCEPTED | `sampling-manifest.input_digest` intentionally includes the concrete upstream `inventory_ref`, so a new inventory identity may change it. Inventory digests stay stable when only inventory envelope identity/time changes, and `sampling-manifest.output_digest` stays stable for the same selection semantics. The `input_digest` change is provenance binding, not nondeterminism. | Product Owner closeout disposition, 2026-09-19 (same brief as `D-PXAPI19-PO-011`; not yet recorded in Jira) | Resolves `C-PXAPI-010` by documentation authority: `contracts/README.md` wording corrected, **no** code, schema or test change. Bounds the final clause of `D-PXAPI19-PO-004` to each document's own envelope. Pinned by `tests/application/test_discover_site.py::test_a_new_identity_and_instant_change_no_digest_but_the_manifest_input_binding`. |

The `D-PXAPI19-PO-001` … `-007` rows above are an index of Product Owner decisions recorded in the Jira item and the cited Confluence pages during PXAPI-19 planning. They are indexed here because two committed evidence documents and several source modules cite them by id while this ledger carried none of them. The cited authority, not this index, is the decision.

Rows `D-PXAPI19-PO-008` … `-012` index the PXAPI-19 closeout decisions (2026-09-17 … 2026-09-19). `-008` … `-010` cite Jira comments that exist. `-011` and `-012` cite the Product Owner's 2026-09-19 closeout disposition, which at the time of writing exists as the brief for the closeout PR and **not yet as a Jira record**; until Jira `PXAPI-19` carries it and has been read back, those two rows are the repository's index of a decision whose authoritative record is still to be written. The evidence record for all five is `docs/evidence/PXAPI-19-final-closeout.md`.

## Change rule

A new decision may be appended only when its authority and scope are explicit. A newer artifact does not silently supersede an older authorized product decision. Supersession must name the prior decision and the bounded scope it changes.