"""Runtime access to the versioned contract registry.

JSON Schema is the contract authority. This package reads the registry as *data* and validates
documents against it; it defines no shape of its own, so there is nothing here for a schema to
disagree with.
"""
