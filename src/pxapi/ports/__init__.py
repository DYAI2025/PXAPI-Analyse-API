"""Ports — the explicit boundaries the application talks through.

May import: the standard library, ``pxapi.domain`` and ``pxapi.ports``.

Empty in A3 on purpose. A port is a business contract, and the contracts belong
to A4/A5. Declaring speculative protocols here would invent exactly the business
surface this slice is required not to invent.
"""
