# PXK-14 — Repository Preflight and Frozen Start Epoch

**Jira key:** PXK-14 (PXAPI A1 Repository preflight and frozen start epoch)
**Parent:** PXK-8 — PXAPI Foundation & Boilerplate
**UTC verification timestamp:** 2026-08-31T19:33:46Z
**Result:** **PASS**

This document evidences one trustworthy PXAPI start epoch. It deliberately separates three
different claims that are easy to conflate: the canonical source baseline (A), the cleanliness of
the project source worktree (B), and the boundary around local agent-runtime state (C).

---

## A. Canonical repository source baseline

| Property | Verified value |
| --- | --- |
| Repository identity | `DYAI2025/PXAPI-Analyse-API` |
| Origin remote form | `https://github.com/DYAI2025/PXAPI-Analyse-API.git` (approved, credential-free) |
| Local path check | PASS — expected user-supplied repository path verified; absolute workstation path intentionally omitted from public evidence |
| Valid Git worktree | yes (`git rev-parse --is-inside-work-tree` → `true`) |
| Worktree top-level | equals the verified repository path (cwd is the worktree root) |
| Verified branch | `main` |
| Attached HEAD | yes (`git symbolic-ref -q HEAD` → `refs/heads/main`; not detached) |
| Verified local HEAD | `3978792dac11abd5fda0b470de21d11c28812515` |
| Verified remote default branch | `main` (live, via `git ls-remote --symref origin HEAD`) |
| Verified remote SHA | `3978792dac11abd5fda0b470de21d11c28812515` |
| Frozen source epoch | `3978792dac11abd5fda0b470de21d11c28812515` |

**Three-way equality holds:** local HEAD == live remote `main` == frozen source epoch.

The remote default branch and remote SHA were resolved by live network read (`ls-remote`), not
from local remote-tracking state. At this epoch `git ls-files` reports exactly one tracked file
in the entire repository: `LICENSE`.

## B. Project worktree cleanliness

Immediately before this document was written, `git status --porcelain=v1 --untracked-files=all`
returned **no lines**. Therefore, at that moment:

- there were **no tracked changes** (`git diff HEAD --stat` empty; `LICENSE` unmodified);
- there were **no unexpected untracked project files**.

This is a statement about **project source state as Git reports it**. It is deliberately *not* a
claim that the physical directory contains no tool-runtime files — see section C, which documents
one such file that does exist on the verifying workstation.

## C. Local agent-runtime boundary

The verifying workstation runs a local Claude Code agent whose PostToolUse hook continuously
regenerates a telemetry log inside the worktree:

- **Path:** `.claude/homunculus/observations.jsonl`
- **Nature:** generated by local Claude tooling. Inspected and classified before exclusion: every
  record is a JSON line of type `tool` or `prompt` recording tool invocations and prompt text.
  It is **agent runtime telemetry, not PXAPI project source**, and it is regenerated on
  essentially every agent tool call.
- **Tracking status:** not tracked source. It has never been added, committed or pushed.
- **Isolation mechanism:** excluded locally through one exact rule appended to
  `.git/info/exclude`, matching that single full path and nothing else.
- **Scope of that mechanism:** `.git/info/exclude` is **workstation-local Git metadata**. It is
  not versioned, not committed, not pushed, and not shared with GitHub or with any other clone.
  It is **not** a committed PXAPI policy, and no repository `.gitignore` was created or modified.
- **Observability:** the exclusion classifies, it does not conceal. The file remains directly
  inspectable:

```
$ git check-ignore -v .claude/homunculus/observations.jsonl
.git/info/exclude:7:.claude/homunculus/observations.jsonl	.claude/homunculus/observations.jsonl

$ git status --ignored --short .claude/homunculus/observations.jsonl
!! .claude/homunculus/observations.jsonl
```

- **Narrowness (verified, not assumed):** any other `.claude` path still surfaces normally as
  ordinary untracked state. `git check-ignore` returns non-zero for
  `.claude/homunculus/instincts/pending`, `.claude/other.txt`,
  `.claude/homunculus/other.jsonl` and `CLAUDE.md`. Only the one exact telemetry path is
  isolated.

## Reproducible command record

Preflight (all exited 0):

```
pwd -P
git rev-parse --is-inside-work-tree
git rev-parse --show-toplevel
git remote get-url origin        # read into a shell variable and matched against approved
                                 # credential-free forms before any output
git branch --show-current
git symbolic-ref -q HEAD
git rev-parse HEAD
git status --porcelain=v1 --untracked-files=all      # returned no lines
git ls-files                                          # LICENSE
git ls-remote --symref origin HEAD
git ls-remote origin refs/heads/main
```

Local mutations performed for this task, in order:

```
rm docs/evidence/PXK-14-repository-preflight.md       # removed a superseded draft of this file
printf '%s\n' '.claude/homunculus/observations.jsonl' >> .git/info/exclude
git check-ignore -v .claude/homunculus/observations.jsonl
git status --ignored --short .claude/homunculus/observations.jsonl
```

The append preserved the pre-existing stock `.git/info/exclude` content; `diff` of the file
before and after reports exactly one added line (`6a7`), and the file now contains exactly one
non-comment rule.

No `fetch`, `pull`, `reset`, `clean`, `stash`, `rebase`, `checkout`, `switch`, `branch`, `add`,
`commit` or `push` was executed. `git reflog` contains a single entry — the original clone — so
no ref or history was mutated.

## Scope statement

**No application behavior was changed.** PXK-14 created no application code, packages,
dependencies, contracts, schemas, CI or deployment configuration, and did not modify `LICENSE`.

The only project-source candidate created by PXK-14 is this evidence document. A
workstation-local `.git/info/exclude` entry isolates the separately verified Claude runtime log
from project dirty-state checks.

## Claim boundary

This preflight proves:

- exact repository identity (`DYAI2025/PXAPI-Analyse-API`) and a valid worktree;
- exact branch `main` with an attached HEAD;
- the exact frozen source epoch, and equality between local HEAD, live remote `main` and that
  epoch;
- project-source baseline cleanliness at verification time, as defined in section B;
- an explicit, narrow, observable separation of known agent-runtime state, as defined in
  section C.

This preflight does **not** prove: boilerplate correctness, contract or schema validity, test or
CI health, deployment or runtime behavior, Regression Oracle readiness, or the readiness of any
later Foundation slice. It is not a merge-readiness statement, and it does not close PXK-14 in
Jira.
