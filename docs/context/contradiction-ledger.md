# PXAPI Contradiction Ledger

This ledger preserves unresolved, historical or bounded cross-source divergences. It prevents a newer or more convenient source from silently laundering conflicting evidence into truth.

| ID | Status | Claim / contradiction | Sources | Resolution / current treatment |
| --- | --- | --- | --- | --- |
| C-PXAPI-001 | OPEN / NON-BLOCKING FOR PXAPI-19 | GitHub PR #5 remains open although current `main` has independently evolved far beyond its old base. | PR #5 base `b84a283...`, head `ed9814b...`; current main lineage through governance PRs and PXAPI-19.A | Treat PR #5 as stale legacy delivery drift. It is not current WIP and must not be merged without a separate reconcile. No automatic close/delete. |
| C-PXAPI-002 | RESOLVED BY AUTHORITY BOUNDARY | Historical PXK Jira issues describe earlier API slices while current project authority is Jira project `PXAPI` / Board 436. | Historical PXK tickets; PXAPI Confluence Jira-authority page `52625409`; current PXAPI backlog | Historical PXK items are reference/evidence only. New backlog/status/sprint truth uses PXAPI Jira. |
| C-PXAPI-003 | RESOLVED BY CURRENT/TARGET SPLIT | Skill V2.1 contains scoring, customer PDF, legal sidecar and batch capabilities that current PXAPI `main` does not implement. | PXK-web-skill docs / frozen V2.1 snapshot; PXAPI README/current contracts | This is expected parity gap, not false implementation. Skill is Regression Oracle / migration source; PXAPI target architecture may adopt/adapt capabilities incrementally. |
| C-PXAPI-004 | RESOLVED BY SOURCE BINDING | Confluence product amendment described the installed Skill baseline while GitHub `pxk-web-skill` contains multiple uploaded ZIPs. | PXAPI amendment `52527106`; parity page `54362137`; private repo `DYAI2025/pxk-web-skill@f29767f...` | `PXAPI-4` bound parity to exact V2.1 artifact/digests and is Erledigt. Any later Skill version requires a new parity rebaseline; unqualified repo `latest` remains invalid as parity authority. |
| C-PXAPI-005 | OPEN / DESIGN INPUT | Target acquisition architecture requires census-first sampling but the exact census-to-sampling threshold is unknown. | PXAPI architecture `54362115`; PXAPI-19 | Keep threshold `MISSING` until representative benchmarks support a versioned policy. Do not invent a threshold in implementation. |
| C-PXAPI-006 | OPEN / IMPLEMENTATION-TIME REVERIFY | Crawl4AI was evaluated at version `v0.9.3`, but provider facts are time-sensitive. | PXAPI architecture `54362115` | Reverify current upstream version, security notes and license/attribution terms before PXAPI-21 implementation. Do not treat the evaluation version as permanently current. |
| C-PXAPI-007 | OPEN / NON-BLOCKING FOR PXAPI-19 CLOSEOUT | Jira raw issue-link data for `PXAPI-19` exposes `PXAPI-20` and `PXAPI-21` as `inwardIssue` values under link type `Blocks` / `is blocked by`, which implies PXAPI-19 is blocked by those downstream items. This conflicts with accepted product sequencing and PXAPI-20's own dependency text requiring full PXAPI-19 first. Teamwork Graph can render the same relationship as PXAPI-19 blocking PXAPI-20/PXAPI-21, so direction semantics are not safely normalized across views. | Jira `PXAPI-19` links `11503`, `11504`; Jira `PXAPI-20` dependency text; Confluence `54362115`; product sequence D-PXAPI-016 | Preserve as explicit drift. Do not reverse/delete/recreate links until a safe reversible Jira link operation and unambiguous direction semantics are independently verified. This does not block 19.B because the authoritative execution order remains PXAPI-19 -> PXAPI-20 and PXAPI-20 stays unauthorized until full PXAPI-19 closeout. |

| C-PXAPI-008 | RESOLVED BY GOVERNANCE DECISION / HISTORICAL EXCEPTION PRESERVED | The merge commit `fe8d838023c8f9967151cad9aa1981e76dcf7d75` states PR #16 was merged "after exact-head R4M", while the candidate it merges says the opposite. No independent exact-head `R4M` artifact for `7e5d67eb95f85216eff32d99ff82227cb5786efc` has been located in this repository. | Merge commit `fe8d838` message; PR #16 body ("`R4M` is **not** declared by this PR"); candidate tip commit `7e5d67e` message ("`R4M` is not declared here"); `docs/evidence/PXAPI-19B-site-discovery-runtime.md` lines 14-15 and 690 ("**MISSING:** an independent `R4M`"); GitHub PR #16 reviews (one, from `sourcery-ai[bot]`, state `COMMENTED`, declining review over its 150,000-character diff limit; zero issue comments, zero review comments); `main` has no branch protection | Measured on `fe8d838`: `git grep -l '7e5d67eb95f85216eff32d99ff82227cb5786efc' HEAD` returns 0 tracked files and `grep -c '7e5d67e' docs/evidence/PXAPI-19B-site-discovery-runtime.md` returns 0. A merge commit message is the merging party's own assertion and is not independent evidence under Confluence `55181314`. **Resolved operationally, not evidentially, by Jira `PXAPI-19` comment `16044` (2026-09-17):** of the two lawful paths, the second was taken — PR #16 merged **without** independently evidenced exact-head R4M, and that is recorded rather than repaired. The historical gate state is **`R4M = UNKNOWN`, permanently, and never `GREEN`**; **no retrospective approval is manufactured**, now or later. The contradiction is preserved, not deleted: the merge-commit claim, PR #16's own non-declaration, the candidate tip's non-declaration, the absent exact-head artifact, Sourcery's refusal to review a 340,448-byte diff and the absent branch protection all stay on this row as the record of how the gap arose. Consequences: the merge is not invalidated; no claim of full DRS conformance for the 19.B merge process is available, permanently; this row no longer blocks PXAPI-19 closeout; and it authorises nothing — `PXAPI-20` stays blocked and `C-PXAPI-009` (AC8 evidence class) is untouched. **2026-09-19 note:** `C-PXAPI-009` was later resolved on its own authority (see its row), and PR #18 carried its own exact-head `R4M` (`D-PXAPI19-PO-010`). Neither alters this row: the historical PR #16 `R4M` stays `UNKNOWN`, permanently. |
| C-PXAPI-009 | RESOLVED — FRESH REAL-BOUNDARY REPRODUCTION ACCEPTED | PXAPI-19 AC8 requires a controlled public Real-Boundary-Smoke. The committed mechanism and the committed execution record are different evidence classes, and only the mechanism is independently checkable from this repository. | `tests/smoke/test_real_boundary_smoke.py` (opt-in via `PXAPI_REAL_BOUNDARY_SMOKE_URL`, skipped in CI); `docs/evidence/PXAPI-19B-site-discovery-runtime.md` section D; that document's own definition of `VERIFIED` as author-re-derived (lines 16-17); resolution: `D-PXAPI19-PO-011`, `docs/evidence/PXAPI-19-final-closeout.md`, `docs/evidence/PXAPI-19-AC8-real-boundary-receipt.json` | **Resolution, 2026-09-19 — Product Owner closeout disposition (`D-PXAPI19-PO-011`).** AC8 is accepted for ticket closeout on a **fresh** reproduction: an attached clean `main` checkout at `bfe06cc07ea0dfde31fedb8e7115940da24fb9fd`, target `https://www.rfc-editor.org/`, Python 3.13.3 and 3.14.6. The opt-in smoke passed on both; in both captured envelopes all six documents have zero schema violations and the site inventory and sampling manifest — the only contracts with semantic and producer rules — have zero of those; each shows an established canonical origin, 506 candidates, all 506 `ELIGIBLE`, `CENSUS`, 506 selections, `selection_complete = true`, and `SITEMAP = BUDGET_EXHAUSTED` at the declared 500-entry bound (`max_sitemap_entries`), which stays a technical source outcome and produces no finding, score or severity. The machine-readable receipt is the repository's first non-prose AC8 record. It is a derived record: its contract and digest values were re-computed from the captured envelopes, and its exit codes and test summaries were read from the run's log files. Neither the envelopes nor the logs are committed, and their retention is not guaranteed. **This is a new reproduction and not a retroactive upgrade:** the historical 2026-09-11 record in `docs/evidence/PXAPI-19B-site-discovery-runtime.md` section D keeps the class this row gave it and stays unedited. **Evidence ceiling:** one controlled public-origin Discovery → SiteInventory → SamplingManifest boundary — not complete site coverage, arbitrary crawling, production scale, browser rendering, PXAPI-20, scoring quality, customer output or customer value. **Prior state, preserved:** `OPEN / BLOCKING FOR PXAPI-19 CLOSEOUT`, with this record: The mechanism is `CODE`/`TEST` and present. The execution record — target origins, the 506/506/506 figures, the four `sha256` digests, the request logs — is prose in one markdown file. Measured: `git ls-files docs/` matches 0 `.json`/`.txt`/`.jsonl`/`.log`/`.csv`/`.har` files, and the sole tracked file carrying the recorded rfc-editor.org inventory `output_digest` is that same markdown. The document states its run driver is scratchpad tooling outside the repository. Treat the section-D figures as `DOC_PERSISTED` / agent-reported. A new execution is a **new reproduction**, never a retroactive upgrade of the historical record. |
| C-PXAPI-010 | RESOLVED BY DOCUMENTATION AUTHORITY | `contracts/README.md` states that re-emitting the same semantics under a new id leaves **both** digests of **both** acquisition contracts unchanged, and its digest table marks envelope ids as never participating. `sampling-manifest.input_digest` is computed over `inventory_ref`, which is the inventory's per-run `inventory_id`, so it changes on every run. | `contracts/README.md` digest-topology table and the sentence "Re-emitting the same semantics under a new id at a new instant leaves both digests unchanged, which is what makes them comparable across runs"; `src/pxapi/domain/acquisition_digests.py` `MANIFEST_INPUT`; `src/pxapi/application/discover_site.py` (`"inventory_ref": inventory["inventory_id"]`); `tests/application/test_discover_site.py::test_a_new_identity_and_instant_change_no_digest_but_the_manifest_input_binding`; Jira-recorded `D-PXAPI19-PO-004` ("Digests exclude envelope IDs/timestamps"); resolution: `D-PXAPI19-PO-012` | **Resolution, 2026-09-19 — Product Owner closeout disposition (`D-PXAPI19-PO-012`), by documentation authority.** The implementation is intentional: `sampling-manifest.input_digest` includes the concrete upstream `inventory_ref`, so a new inventory identity may change it. The intended truth, now stated in `contracts/README.md`: inventory digests stay stable when only inventory envelope identity/time changes; `sampling-manifest.output_digest` stays stable for the same selection semantics; `sampling-manifest.input_digest` may change when a digest-relevant upstream input such as `inventory_ref` changes, and that change is **provenance binding, not nondeterminism**. The README's digest table and its over-broad "leaves both digests unchanged" sentence were corrected; `D-PXAPI19-PO-004`'s final clause is bounded to each document's own envelope. **No code, schema, example or test changed**, and the pinning test named here is preserved. Live corroboration on `bfe06cc`: the two accepted AC8 envelopes, about 70 seconds apart, carry identical inventory `input_digest` and `output_digest`, identical manifest `output_digest` and identical selections, and different manifest `input_digest` values; `inventory_ref` is the only `MANIFEST_INPUT` member that differs. **Prior state, preserved:** `OPEN / DOCUMENTATION AUTHORITY`, with this record: The behaviour is deliberate and pinned by a named committed test, so the **code** is not in doubt. What is in doubt is which statement is authoritative. Measured on two live runs of the same public origin at `fe8d838`: the inventory `input_digest` and `output_digest` and the manifest `output_digest` were identical across runs, while the manifest `input_digest` differed, and `inventory_ref` was the only differing `MANIFEST_INPUT` member. AC1 as written is satisfied for a frozen input under a fixed identity (`test_the_same_frozen_input_in_any_order_yields_the_same_documents`), but two independent runs do not produce a byte-identical manifest. Resolution requires a Product Owner / contract decision between amending the prose and narrowing `MANIFEST_INPUT`. **Not resolved here, and no code was changed.** |
| C-PXAPI-011 | RESOLVED / HISTORICAL EVIDENCE OVERSTATEMENT PRESERVED | Commit `fd3ad8a`'s message and `docs/evidence/PXAPI-19B-site-discovery-runtime.md` both state that the real-boundary smoke asserts the pair "a discovery source may report `BUDGET_EXHAUSTED` while the selection over the resulting inventory is complete". The committed test asserts only the selection half. | `docs/evidence/PXAPI-19B-site-discovery-runtime.md` ("The smoke test asserts both on every run"); commit `fd3ad8a` message ("the smoke now asserts exactly that pair"); `tests/smoke/test_real_boundary_smoke.py`; resolution: Product Owner closeout disposition 2026-09-19, `docs/evidence/PXAPI-19-final-closeout.md` | **Resolution, 2026-09-19 — Product Owner closeout disposition.** Resolved by stating each evidence class for what it is, **not** by rewriting the historical record. (1) The committed smoke **tests the selection-side invariants**: every produced document (run state, stage executions, site inventory, sampling manifest) satisfies its contract, the run `SUCCEEDED`, exactly one canonical seed, `CENSUS`, one selection per eligible candidate, `selection_complete = true`, no `budgets`, no `incompleteness`, no `SELECTION_BUDGET_EXHAUSTED` exclusion, dense ranks, the manifest bound to the inventory's `output_digest`, and no verdict key. (2) The newer structured Real-Boundary evidence of `C-PXAPI-009` separately **observed** `SITEMAP = BUDGET_EXHAUSTED` in both accepted envelopes. (3) **Observation evidence and test assertion are different evidence classes:** the observation does not become a test assertion, and the smoke still does not assert the discovery-side half — re-measured on `bfe06cc`, `grep -cE 'assert[^#]*\b(sources\|SITEMAP)\b' tests/smoke/test_real_boundary_smoke.py` returns 0. Commit `fd3ad8a`'s message and `docs/evidence/PXAPI-19B-site-discovery-runtime.md` stay unedited; their overstatement is preserved here as the record. **Prior state, preserved:** `OPEN / EVIDENCE OVERSTATEMENT`, with this record: Measured on `fe8d838`: `grep -cE 'assert[^#]*\b(sources\|SITEMAP)\b' tests/smoke/test_real_boundary_smoke.py` returns 0, and `git show fd3ad8a -- tests/smoke/test_real_boundary_smoke.py \| grep '^+' \| grep -cE '\bsources\b\|SITEMAP'` returns 0. The test never reads `inventory["sources"]`; the discovery-side truth appears only in a comment. The selection-side assertions are real and pass. The historical documents are **preserved unedited**; this row records the overstatement so no later reader treats the discovery-bound assertion as tested. |
| C-PXAPI-012 | OPEN / SAFETY RESIDUE, OUT OF PXAPI-19 SCOPE | The 19.B repair `F-PXAPI19B-R4M-002` established that a credential-bearing `target_url` must never become emitted output. That guard exists on the discovery path only. The pre-existing homepage analysis path still copies such a target verbatim into the envelope the HTTP API returns as its response body. | `src/pxapi/application/discover_site.py` (guard present); `src/pxapi/application/analyze_homepage.py` and `src/pxapi/adapters/inbound/http_api.py` (guard absent; `return JSONResponse(status_code=200, content=envelope)`); `contracts/v1/schemas/common.v1.json` `$defs/url`, which permits userinfo | Reproduced live on `fe8d838` with `https://user:secret@example.com/`: the request validates against `REQUEST_CONTRACT`, and `"secret" in json.dumps(envelope)` is `True` for the homepage path while the same input on the discovery path is refused with the request withheld. Measured: `grep -c 'CREDENTIALS_PRESENT\|_carries_credentials\|site_identity' src/pxapi/application/analyze_homepage.py` returns 0, same for `http_api.py`, and no test drives userinfo through either. This belongs to the PXK-67 / PXK-20.B homepage slice, **not** to PXAPI-19, and it was not introduced by 19.B. It does not fail any PXAPI-19 acceptance criterion. Recorded here so the invariant's residue is visible; it needs its own authorized ticket. |

| C-PXAPI-013 | CLOSED — repaired by PR #18 | `html_links.read_links()` promises that malformed markup is ordinary input read tolerantly, and that only a document the parser cannot process raises `HtmlUnreadable`. Its guard covers the parser feed loop alone; `_base_for(...)` and the `urljoin(base, target)` loop run outside it, so one malformed `<base href>` or `href` raises after parsing and costs the document every one of its links. | `src/pxapi/adapters/web/html_links.py` module docstring ("Unclosed tags, stray markup and links nested in ways the standard forbids are ordinary input and are read tolerantly") vs. the placement of its `try/except Exception -> HtmlUnreadable`; `src/pxapi/adapters/web/site_discovery.py` `_links` (catches `HtmlUnreadable` only) and `_guarded` (broad `except Exception`); Jira `PXAPI-19` comment `16045` finding 1; resolution: Jira `PXAPI-19` comments `16081` (`AD-008`) and `16082` (`AD-009`), PR #18, `docs/evidence/PXAPI-19B-discovery-correctness-repair.md` | **Resolution, 2026-09-18 — closed, repaired by PR #18** (`D-PXAPI19-PO-009`; merged in `bfe06cc` under `D-PXAPI19-PO-010`). `html_links.py` now isolates each unreadable `<base href>` and each unresolvable `href` with a narrow `except ValueError`, so one malformed value costs one link, never its valid siblings. 17 tests were RED against base `aa75d6b` and pass on the candidate and on `main` (post-merge CI `35342958323`). The same defect class in the sibling reader `_robots()` (NF-5) was closed on the same candidate: one unresolvable `Sitemap:` declaration costs one declaration, a malformed-only file is neutral `MALFORMED`, and truncation keeps `BUDGET_EXHAUSTED` precedence. Evidence: the repair document §2, §3 and §12. Residue, untriaged and outside the discovery theme, not a PXAPI-19 acceptance blocker: the same unguarded `urljoin` at `src/pxapi/application/analyze_homepage.py:478`; and NF-6, `EMPTY` for a document whose links are all unresolvable. (The repair document also lists `src/pxapi/adapters/web/page_fetcher.py:168`; re-read at `bfe06cc`, that call already sits inside `try: … except ValueError` and returns `INVALID_REDIRECT`, so it is not a residue. The repair document stays unedited.) **Prior state, preserved:** `OPEN / IMPLEMENTATION CORRECTNESS — ACCEPTANCE BLOCKER FOR PXAPI-19`, with this record: Reproduced on `fe8d838`: `read_links(b'<a href="/ok">good</a><a href="http://[">bad</a><a href="/ok2">good2</a>', ...)` raises a raw `ValueError: Invalid IPv6 URL`; `<base href="//[">` and `<a href="//[2001:db8">` do the same; the all-good control returns 2 links. The raw `ValueError` is not caught by `_links`' `HtmlUnreadable` handler but by `_guarded`'s broad `except Exception`, so the outcome is `SAME_ORIGIN_PAGE_LINKS = RUNTIME_ERROR` with zero observations. Neutrality is preserved — `RUNTIME_ERROR` blames our runtime, never the site — so this is an **inventory-completeness** defect, not an AC5/AC6 polarity defect. Repair inside authorized 19.B scope; no new ticket. See AD-007 A. |
| C-PXAPI-014 | CLOSED — repaired by PR #18 | `URL_CANONICAL_BASELINE` and `site_identity`'s own comment state that the fragment is dropped without being examined, while `_canonicalise()` scans the entire raw written form for forbidden characters before `urlsplit`. A URL with a valid page identity and a space in its fragment is therefore refused, and the page is lost from the inventory. | `src/pxapi/domain/site_identity.py` `_canonicalise` and its rule-6 docstring ("The fragment is never examined: it is dropped"); `URL_CANONICAL_BASELINE 1.0.0`; Jira `PXAPI-19` comment `16045` finding 2; resolution: Jira `PXAPI-19` comments `16081` (`AD-008`) and `16082` (`AD-009`), PR #18, `docs/evidence/PXAPI-19B-discovery-correctness-repair.md` | **Resolution, 2026-09-18 — closed, repaired by PR #18** (`D-PXAPI19-PO-009`; merged in `bfe06cc` under `D-PXAPI19-PO-010`). `_canonicalise()` now splits the fragment off before the forbidden-character scan, matching rule 6 of `URL_CANONICAL_BASELINE 1.0.0`; 21 tests were RED against base `aa75d6b` and pass on the candidate and on `main`. Residue, which comment `16081` classifies as a separate contract / observed-form boundary and not as a remaining implementation contradiction: a fragment carrying a raw ASCII space or tab still makes the *observed form* non-persistable under `acquisition#/$defs/public_url` (NF-2). The judgement not to bump `CANONICALISATION_VERSION` is recorded in the repair document §9 and is not re-decided here. **Prior state, preserved:** `OPEN / IMPLEMENTATION CORRECTNESS — ACCEPTANCE BLOCKER FOR PXAPI-19`, with this record: Reproduced on `fe8d838`: `canonical_url_key('https://example.com/leistungen#unsere leistungen')` returns `None` and `refuse(...)` returns `FORBIDDEN_CHARACTER`, while `'https://example.com/page#plain'` canonicalizes normally to `https://example.com/page`. A hand-written German SME anchor of that shape drops the page entirely — the error `site_identity` names as the one it must not make, since a lost page is invisible in the inventory rather than visibly refused. Stripping the fragment before the character scan preserves both the refusal semantics and the page. Repair inside authorized 19.B scope; no new ticket. See AD-007 B. |
| C-PXAPI-015 | SUPERSEDED AS IMPLEMENTATION DEFECT | `HttpSiteDiscovery` records `CANONICAL_SEED` as `SourceOutcome.USED` unconditionally at origin bootstrap, without consulting the seed's HTTP status, while the link source derives its own outcome from that same status. A technically unsuccessful seed response is therefore reported as a successfully used source. | `src/pxapi/adapters/web/site_discovery.py` (`attempts = [SourceAttempt(SourceId.CANONICAL_SEED.value, SourceOutcome.USED)]`) vs. `_links`' `_status_outcome(seed.status_code)` two lines later; PXAPI-19 AC6; Jira `PXAPI-19` comment `16045` finding 3; resolution: Jira `PXAPI-19` comment `16081` (`AD-008`), `docs/evidence/PXAPI-19B-discovery-correctness-repair.md` §2 | **Resolution, 2026-09-18 — superseded as an implementation defect** (`D-PXAPI19-PO-009`). The accepted canonical-seed semantics are preserved: `CANONICAL_SEED` is the origin the bootstrap established, not the seed HTTP document, and the document's HTTP status is reported by `SAME_ORIGIN_PAGE_LINKS` — `ABSENT` for a 404, `PROVIDER_FAILURE` for a 500, `MALFORMED` for a non-HTML seed — pinned by `test_a_seed_that_is_not_a_readable_page_still_establishes_the_origin`. Deriving the seed outcome from a 4xx/5xx status is contract-impossible: `site-inventory.v1` `sources[].allOf[0]`, `candidates.minItems: 1` and the producer rule `seed_candidate_present` together make every such inventory unemittable (`SITE_INVENTORY_NOT_EMITTABLE`), replacing an honest one-candidate inventory with no inventory at all, as measured in the repair document. **No code or contract change.** Residue, a contract question and not a PXAPI-19 acceptance blocker: no vocabulary exists for "origin established, seed document answered 404" (NF-1). **Prior state, preserved:** `OPEN / IMPLEMENTATION CORRECTNESS — ACCEPTANCE BLOCKER FOR PXAPI-19`, with this record: Confirmed by reading the merged source on `fe8d838`: `seed.status_code` is never consulted on the seed's own source row, though `_status_outcome` is already in scope at that point. A homepage answering 500 still establishes an origin, so the inventory emits `CANONICAL_SEED / USED / 1` and the run reports `SUCCEEDED` with that seed `ELIGIBLE` and selected at rank 1; the only trace of the failure is the *links* row. This is the one finding of the four that bears directly on **AC6** — a technical state must be modelled truthfully — rather than only on completeness. Repair inside authorized 19.B scope; no new ticket. See AD-007 C. |
| C-PXAPI-016 | NON_MATERIAL AS IMPLEMENTATION DEFECT | Two named `DiscoveryLimits` members do not bound the quantities the implementation and the 19.B evidence prose imply they bound: `max_sitemap_documents` does not stop the declaration loop for off-origin `Sitemap:` references, and `max_page_links` bounds distinct written `href` forms while observations are persisted per `(href, label)`. | `src/pxapi/adapters/web/site_discovery.py` (`plan(url)` — the off-origin branch returns before the queue-size gate, so `budget_cut` is never set and `for url in declared: ... if budget_cut: break` never breaks; the link loop — `forms` is what `max_page_links` caps, `seen`/`observed` are keyed on `(href, label)`); commit `09b716a` ("no hostile input can outrun a declared bound"); `docs/evidence/PXAPI-19B-site-discovery-runtime.md`; Jira `PXAPI-19` comment `16045` finding 4; resolution: Jira `PXAPI-19` comment `16081` (`AD-008`), `docs/evidence/PXAPI-19B-discovery-correctness-repair.md` §4 | **Resolution, 2026-09-18 — non-material as an implementation defect, under the currently authoritative named-limit definitions** (`D-PXAPI19-PO-009`). `max_sitemap_documents` bounds sitemap documents **read** and `max_page_links` bounds distinct written same-origin link **targets** admitted (`src/pxapi/config/discovery_limits.py`). Executable counterexamples show both honoured — 20 000 off-origin declarations read one document, and 300 labels on one `href` yield one candidate — and the response-byte and deadline bounds stay in force (worst cases 2.108 s and 10.443 s against a 60 s deadline, on the repair author's machine). The locks in `tests/adapters/test_discovery_bounds.py` were green before and after. **No code change.** The historical prose that implied different quantities — commit `09b716a` and `docs/evidence/PXAPI-19B-site-discovery-runtime.md` — is not implementation authority and stays unedited. The verdict must be re-measured if an authority restates what a named limit bounds. **Prior state, preserved:** `OPEN / IMPLEMENTATION CORRECTNESS — ACCEPTANCE BLOCKER FOR PXAPI-19`, with this record: Confirmed by reading the merged source on `fe8d838`. **Execution stays bounded** and this row asserts no unbounded runtime: `FetchLimits.max_response_bytes` (2 MiB) caps both the robots.txt body that carries the declarations and the seed body that carries the anchors, so both quantities have an outer bound. The defect is narrower and exact: the *named* limits do not mean what they are documented to mean, and a source whose observation count outgrew `max_page_links` still reports `USED` rather than `BUDGET_EXHAUSTED`. Commit `09b716a`'s claim is therefore true of the paths that have a declared bound and overstated as written. Repair inside authorized 19.B scope; no new ticket. See AD-007 D. |
| C-PXAPI-017 | OPEN / NON-BLOCKING FOLLOW-UP CANDIDATE — NOT A PXAPI-19 ACCEPTANCE BLOCKER | `analysis-run-state.v1` describes `entered_at` as "When the run entered the state it is currently in", while terminal run states are emitted with `entered_at` set to the run's **start** instant, so a terminal state can claim to have been entered before the run finished. | `contracts/v1/schemas/analysis-run-state.v1.json` `entered_at` description; at `bfe06cc`: `src/pxapi/application/discover_site.py` lines 229-230 (`SUCCEEDED`) and 390-391 (`FAILED`) and `src/pxapi/application/analyze_homepage.py` lines 221-225 (`FAILED`) and 378 (`SUCCEEDED`), each passing `started` as `entered_at`; accepted AC8 envelope A: `SUCCEEDED`, `entered_at 2026-09-18T21:51:55Z`, `finished_at 2026-09-18T21:51:57Z`; the registered terminal examples (`succeeded`, `failed`, `cancelled`) all carry `entered_at == finished_at` | Surfaced by the final AC8 verification as follow-up candidate **F-1**, recorded 2026-09-19 at the Product Owner's direction as **non-blocking**. The observed run-state documents — both accepted AC8 envelopes and the registered terminal examples — are schema-valid, but schema validity alone proves no DRS gate: not IC, not R2G, not R4M, and not ticket completion. No test pins terminal `entered_at`. The row is non-blocking for PXAPI-19 because it affects none of AC1–AC9, which concern inventory, manifest and neutrality semantics rather than run-state timing, and no applicable PXAPI-19 gate requires terminal `entered_at` semantics beyond the already validated contract shape. Whether the producers or the description should change is a contract / product decision; **nothing is repaired here**, and it needs its own authorised ticket. |

The `C-PXAPI-013` … `C-PXAPI-016` rows were recorded on 2026-09-17 as **implementation correctness defects inside already authorized PXAPI-19.B scope**, and at that time they were why `19.B IC` was `NOT_GREEN — REPAIR REQUIRED` and `19.B R2G` was `NOT_GREEN` (Jira `PXAPI-19` comment `16045`). That is **historical**: comment `16081` dispositioned all four on 2026-09-18 — two closed by the PR #18 repair, one superseded as an implementation defect, one non-material — and the repair merged as `bfe06cc` after its own exact-head `R4M` (comment `16082`). None of the four is a current PXAPI-19 acceptance blocker. They never touched the historical 19.B `R4M`, which stays `UNKNOWN` per `C-PXAPI-008`.

> **Prior wording of this paragraph, preserved (2026-09-17):** The `C-PXAPI-013` … `C-PXAPI-016` rows are **implementation correctness defects inside already authorized PXAPI-19.B scope**, not future-feature backlog. They are why `19.B IC` is `NOT_GREEN — REPAIR REQUIRED` and `19.B R2G` is `NOT_GREEN` (Jira `PXAPI-19` comment `16045`). They get no separate ticket: they are repaired, or explicitly shown non-material, before PXAPI-19 closes. They do not touch the historical 19.B `R4M`, which stays `UNKNOWN` per `C-PXAPI-008`.

## Severity rule

A contradiction blocks mutation only when it affects the same current claim, scope and validity window and cannot be resolved by the correct domain authority. Historical divergence and expected migration gaps remain visible but do not automatically block unrelated work.

## Update rule

Never delete a contradiction to make systems look aligned. Mark it resolved only with the resolving authority/evidence and preserve the prior state.
