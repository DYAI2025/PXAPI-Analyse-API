"""Application layer — orchestration of domain behavior through ports.

May import: the standard library, ``pxapi.domain``, ``pxapi.ports`` and
``pxapi.application``. It may not import ``pxapi.adapters``, and it may not
import third-party distributions.

Empty in A3: there are no use cases yet.
"""
