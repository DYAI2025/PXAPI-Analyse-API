# PXK-67 — Real URL → HTTP Evidence → Analysis JSON (Walking Skeleton)

**Slice:** PXK-67
**Date:** 2026-09-07
**Authority:** Confluence `49709119` — *PXAPI — Business-First Delivery Reconcile — Real
Functionality First — 2026-09-06*; Jira `PXK-67`
**Status of every claim below:** `AGENT_REPORTED`. Each is re-derivable by the command printed
beside it; none has been confirmed by independent PO or GitHub review.

**No production readiness is claimed.** There is no authentication, no authorisation, no rate
limiting, no persistence and no deployment. See section F.

---

## A. Source baseline

| Fact | Exact value | How verified |
| --- | --- | --- |
| Repository | `DYAI2025/PXAPI-Analyse-API` | `git remote -v` |
| Base `main` | `147c63772e05fd84e7970901b3b8105976b80269` | `git fetch && git rev-parse origin/main` |
| Matches the brief's pinned SHA | yes, character-identical | string comparison |
| Drift | none | no reconciliation needed, no STOP condition |
| Branch | `feat/pxk-67-real-url-walking-skeleton` | `git rev-parse --abbrev-ref HEAD` |
| Branch point | `origin/main`, not a working branch | `git checkout -b … origin/main` |
| Baseline suite before any change | `744 passed` | `uv run pytest -q` |

`main` had not moved from the independently verified SHA, so the brief's assumptions held.

### Commits

| SHA | Subject |
| --- | --- |
| `a18f929` | `feat(pxk-67): analyse a real public homepage into canonical contract documents` |
| `7552f80` | `test(pxk-67): prove a real response becomes evidence and a failure never does` |
| `364e2cc` | `test(pxk-67): close the three gaps a countermutation pass exposed` |
| `c205f81` | `fix(pxk-67): decode compressed responses instead of reporting the site as empty` |
| `850819f` | `docs(pxk-67): record the real request path, the target boundary and the smoke` |
| `b16fecf` | `docs(pxk-67): correct the suite size at the moment the smoke found the defect` |
| `791cea5` | `fix(pxk-67): state the URL each fact was actually observed against` |

The first commit was verified green on its own by moving the not-yet-committed test
directories aside and running the suite: `830 passed`.

## B. Project-source cleanliness

`git status --porcelain` reports no entry after the final commit. That is a statement about
what git tracks, **not** a claim that the physical directory holds nothing else — see C.

## C. Local agent-runtime boundary

`.claude/homunculus/` is generated by a local tool on this workstation and regenerates on
essentially every tool call. It is untracked (`git ls-files .claude` prints nothing) and is
isolated through `.git/info/exclude`, which is workstation-local and never shared with GitHub.
`.gitignore` deliberately carries no `.claude/` rule, and none was added here.

`local_path_check: PASS` (the absolute workstation path is deliberately omitted).

## D. What this slice delivers

### The problem it solves

Before this slice `src/pxapi/` held exactly one executable module. PXAPI had a contract
foundation and had never fetched a website. PXK-67 is the first vertical product path.

### The real request path

```text
POST /v1/analysis-runs            adapters/inbound/http_api.py     FastAPI: transport only
  -> json.loads, rejecting NaN/Infinity
  -> registry.validate("analysis-run-request")   JSON Schema is the only contract authority
       violations -> to_problem() -> 422 problem.v1.json
  -> application/analyze_homepage.py             the single use case, one lifecycle
       |- domain/run_state.transition()          CREATED -> QUEUED -> RUNNING -> SUCCEEDED|FAILED
       |- adapters/web/target_policy.py          public-target boundary, before any socket
       |- adapters/web/page_fetcher.py           real HTTP, address-pinned, manual redirects
       |- adapters/web/html_observations.py      stdlib HTMLParser
       \- mapping -> MeasurementRecord + WebsiteEvidence
  -> registry.validate(...) on every produced document
  -> 200 canonical envelope
```

`adapters/inbound/cli.py` drives the same use case through the same composition root, which is
what the real-boundary smoke uses.

### The canonical response

`analysis-run-state` is a closed root and cannot embed stage executions, so the envelope is a
thin transport composition whose parts each validate independently:

```text
analysis_run_request   analysis-run-request.v1.json
analysis_run_state     analysis-run-state.v1.json
stage_executions[]     stage-execution-record.v1.json
measurements[]         measurement-record.v1.json
website_evidence[]     website-evidence.v1.json
```

The envelope is deliberately **not** a registered contract. It adds no analysis meaning, and
registering it would create a second authority beside the contracts it merely carries.

### No contract needed changing

`metric_id`, `collector`, `scenario` and `stage_id` all `$ref` `common#/$defs/code` — open
vocabularies that fix a token's lexical shape and never the set. Every metric this slice
introduces validates against the contracts exactly as merged. `contracts/v1/manifest.json`
gained three `problem_codes` and nothing else; `git diff -U0` reports **0 deletions** in it.

### Truth policy

| Situation | Measurement | Evidence | Run |
| --- | --- | --- | --- |
| Value observed | `KNOWN` + typed `result` | `KNOWN`, no polarity | `SUCCEEDED` |
| Timeout | `TIMEOUT`, no result | polarity refused | `FAILED` |
| DNS / connection failure | `PROVIDER_FAILURE` | refused | `FAILED` |
| Blocked target | `PERMISSION_DENIED` | refused | `FAILED` |
| Parser failure | `RUNTIME_ERROR` (document metrics only) | refused | `SUCCEEDED` |
| Undecodable content encoding | `RUNTIME_ERROR` (document metrics only) | refused | `SUCCEEDED` |
| HTTP 4xx/5xx | status `KNOWN` | `KNOWN`, no polarity | `SUCCEEDED` |
| Element absent | presence `KNOWN false`; value `NOT_APPLICABLE` | `KNOWN`, no polarity | `SUCCEEDED` |
| Non-HTML response | document metrics `NOT_APPLICABLE` | no polarity | `SUCCEEDED` |
| Text over the contract bound | presence `KNOWN`; value `UNKNOWN`, no result | refused | `SUCCEEDED` |
| Body hit the byte bound | not-found elements `UNKNOWN`, never `false` | refused | `SUCCEEDED` |

Three states are kept apart that are easy to conflate: **absent** is a fact about the site,
**not applicable** a fact about the measurement, **unknown** an assessment that established
nothing. No evidence document in any path carries a polarity — every fact here is an
observation, and inventing a good/bad verdict is what the contract's optional polarity avoids.

The `single_line_text` bound is **read out of the schema** at runtime
(`ContractRegistry.max_single_line_text()`, mirroring the existing `max_problem_errors()`), so
no collector holds a copy of the number that could disagree with the contract.

### Observation context after a redirect

`measurement-record.v1` defines `source_url` as *the URL the observation was made against*, and
`website-evidence.v1` as *the URL this evidence is about*. A redirect produces two URLs that
both answer honestly, to different questions, and the first implementation collapsed them: every
record took the request's `target_url`, so after `/from` -> `/final` the status, content type,
transport and every HTML element read off the final response were stated as though the site had
said them at `/from`. Both documents still validated. **A validator cannot check what a URL
means**, which is why this had to be pinned by assertion instead. Fixed in `791cea5`.

The contracts define the meaning but not a metric-by-metric table, so the mapping below is a
bounded Product-Owner interpretation derived from that meaning — not literal schema text.

| Metric | `source_url` context | Why |
| --- | --- | --- |
| `FINAL_URL` | requested target | describes how the *submitted* URL resolved |
| `REDIRECT_COUNT` | requested target | same: an outcome of the path, counted from the start |
| `HTTP_STATUS` | final response URL | read off the response that arrived |
| `CONTENT_TYPE` | final response URL | " |
| `TRANSPORT_IS_HTTPS` | final response URL | " |
| `PAGE_TITLE_PRESENT` | final response URL | " |
| `PAGE_TITLE` | final response URL | " |
| `META_DESCRIPTION_PRESENT` | final response URL | " |
| `META_DESCRIPTION` | final response URL | " |
| `CANONICAL_PRESENT` | final response URL | " |
| `CANONICAL_URL` | final response URL | " |

The final-response context also holds when those metrics become `NOT_APPLICABLE`, performed
`UNKNOWN`, or `RUNTIME_ERROR` after a response was already obtained: the question was still
asked of that response.

`WebsiteEvidence.source_url` is **read off the MeasurementRecord it references** rather than
passed in beside it, so `evidence.source_url == referenced_measurement.source_url` holds by
construction. The divergence is removed rather than corrected. `analysis_run_request.target_url`
is untouched and remains the URL that was submitted.

Where nothing was measured there is no second context, and that path is unchanged: with no
response there is no final URL to name, and per-hop failure provenance is deliberately **not**
redesigned here (see section G).

For a target that does not redirect, requested and final are the same URL, so observable
behaviour is unchanged — which is exactly why 1,102 passing tests did not see this either.

### Public-target boundary

Decided before any socket exists, and again on every redirect hop:

1. `http` and `https` only.
2. Credentials in the authority refused, and never rendered into a message or a log.
3. Hostname resolved; an IP literal skips resolution but never classification.
4. Every address classified: `is_global`, not multicast, not reserved. IPv4-mapped IPv6 is
   unwrapped first, so `::ffff:127.0.0.1` cannot pass as globally routable.
5. **Partial resolution is fatal** — one forbidden address condemns the host.
6. The connection is made to the **validated address**, not the hostname, so a name cannot be
   rebound between the check and the connect. TLS still verifies the certificate against the
   hostname.
7. Redirects are followed by hand; each `Location` is re-validated and re-resolved.

Bounds: 3 redirect hops, 5 s connect, 10 s read, 20 s overall deadline, 2 MiB response. The
read is bounded, and decompression is bounded to the same figure, so a compression bomb cannot
inflate past one bounded body.

The shipped `PublicTargetPolicy` takes **no argument that could widen its address rule**. The
single extension point is a method, so the loopback policy the integration tests need is a
subclass defined in `tests/`, and a test asserts both that the shipped policy still refuses
every loopback address and that its constructor has gained no such parameter.

### The dependency guard was narrowed, not dropped

Runtime validation makes PXK-59's blanket rule untrue, so it is narrowed in the manner of
PXK-60's behavior gate. `domain`, `ports`, `application` and `config` may still never import a
validator — that is the invariant that keeps contract shapes out of business logic. Exactly one
allowlisted adapter may, and an allowlist entry that is stale, unearned or outside `adapters`
fails on its own. `tests/architecture/boundaries.py` needed no change: `adapters` was already
in `THIRD_PARTY_ALLOWED`, and the AC3 ban on `fastapi` only binds outside adapters.

`tests/contracts/support.py` now builds on the runtime registry instead of restating it, so the
validation the tests exercise is the code the service runs. It lost 178 lines of duplication.

## E. Verification

### RED before GREEN

| Step | Command | Result |
| --- | --- | --- |
| Baseline | `uv run pytest -q` | `744 passed` |
| After declaring the runtime dependency | `uv run pytest -q` | `4 failed, 740 passed` — only the isolation guard |
| After the guard was narrowed and modules allowlisted | `uv run pytest -q` | `18 failed, 779 passed` — every failure a module not yet written |
| Target policy, before implementation | `uv run pytest tests/adapters/test_target_policy.py` | `ModuleNotFoundError` |
| Redirect provenance, before the repair (`791cea5`) | `uv run pytest tests/application/test_analyze_homepage.py -q` | `2 failed, 39 passed` |

Two real defects were found by the tests during implementation, before any commit: whitespace
runs were not collapsing (`split(" ")` preserves the empty strings between spaces), and element
*presence* was being decided by whether the value fit the contract bound — which made an
over-long title report as "this page has no title".

### GREEN

| Gate | Command | Result |
| --- | --- | --- |
| Lockfile | `uv lock --check` | `Resolved 29 packages` |
| Lint | `uv run ruff check .` | `All checks passed!` |
| Format | `uv run ruff format --check .` | `56 files already formatted` |
| Full suite, Python 3.13 (primary) | `uv run pytest -q` | `1106 passed` |
| Full suite, Python 3.14 (compat) | `uv run --python 3.14 pytest -q` | `1106 passed` |

The four added tests are the redirect-provenance regression described in section D.

### The gates were observed failing, not merely observed green

Twelve countermutations were applied to real source files, the full suite run, and each
reverted with `git checkout --`. The tests were committed first, so no mutation loop could
delete them; `git status --porcelain` was empty after each pass.

| # | Mutation | Result | Caught by |
| --- | --- | --- | --- |
| M1 | redirect re-validation removed | `15 failed` | the redirect and re-resolution proofs |
| M2 | host accepted if *any* address is public | `2 failed` | the partial-resolution rule |
| M3 | presence follows representability | `1 failed` | the over-long title proof |
| M4 | truncated read reports absence | `1 failed` | the truncation proof |
| M5 | non-HTML parsed as HTML | `1 failed` | the not-applicable proof |
| M6 | read bound dropped (`response.read()`) | **`1050 passed` — SURVIVED** | nothing |
| M7 | over-long text truncated in `representable_text` | **`1050 passed` — SURVIVED** | nothing |
| M8 | TLS connects by hostname | **`1050 passed` — SURVIVED** | nothing |

**Three mutations survived**, and each named a real hole rather than a cosmetic one:

* M6 — the assertions checked the value that was *kept*, not what was pulled off the socket. A
  fetcher that read an unbounded body and then sliced it satisfied every test.
* M7 — the application reaches the bound through `normalise_single_line`, so the helper had no
  direct test at all. The domain had no test module.
* M8 — the mutation hits the **TLS** connect, and every integration test speaks plain HTTP, so
  the entire HTTPS pinning path was unasserted.

After the gaps were closed (`364e2cc`), all three were re-run and a fourth added:

| # | Mutation | Result |
| --- | --- | --- |
| M6′ | read bound dropped | `1 failed` |
| M7′ | over-long text truncated | `2 failed` |
| M8′ | TLS connects by hostname | `1 failed` |
| M9 | TLS verifies the certificate against the pinned address | `1 failed` |

A further round was run against the redirect-provenance repair. Each mutation was applied to a
**verified-clean** copy of the repaired file, not stacked on the previous one: the first restore
in this round silently failed (`cp` had written the pristine copy to `$TMPDIR`, not where the
restore read from), two mutations therefore ran on top of a third, and the resulting reds proved
nothing. The round was discarded and re-run, with each pass printing a contamination check that
counts the other two mutations' markers and requires `0`.

| # | Mutation | Result | What it proves |
| --- | --- | --- | --- |
| P1 | final-response metrics forced back to the requested URL (the original defect) | `2 failed` | the metric mapping is asserted, not incidental |
| P2 | evidence stops reading its context off the record it references | `1 failed` | the evidence link is independently pinned |
| P3 | `PATH_OUTCOME_METRICS` emptied, so `FINAL_URL`/`REDIRECT_COUNT` take the final URL | `2 failed` | the *other* half of the mapping is pinned too |

P2 failing **only** the evidence test, and P3 failing the path-outcome test that P1 leaves
green, is the evidence that the three rules are separately held rather than one assertion
covering for the rest. After the round, `diff` against the pristine copy was empty, `grep` for
every mutation marker across `src/` and `tests/` returned nothing, and the suite was green.

### Reviewability

| Measurement | Command | Value |
| --- | --- | --- |
| Diff size | `git diff --no-ext-diff --unified=0 origin/main...HEAD \| wc -c` | `244423` |
| Diff size excluding `uv.lock` | same, `-- ':!uv.lock'` | `216677` |
| Deletions in `contracts/v1/manifest.json` | `git diff -U0 … \| grep -c '^-[^-]'` | `0` |

The figure grew from `212242` as the evidence document was written (`850819f`, `b16fecf`,
`+26761 B`) and the provenance repair added `8438 B`; no production file grew materially.

**This exceeds the ~140,000-byte convention PXK-59 was measured against, and is flagged rather
than worked around.** The largest contributors are the use case (20,842 B), the fetcher suite
(19,910 B), the use-case suite (19,485 B) and `uv.lock` (27,746 B). Splitting the FastAPI
transport into a follow-up PR — `adapters/inbound/` plus `test_http_api.py` and the framework
dependencies — removes roughly 38,000 B and would still leave the slice above the figure, so it
would not bring the change under the bound while it would deliver an incomplete vertical path
against the acceptance criteria. Reducing it further would mean deleting proven security tests,
which is not a trade this document will make quietly. **How to proceed is the Product Owner's
call.**

## F. Scope

### Explicit confirmation of what was NOT built

No PostgreSQL, SQLAlchemy, S3 or object storage. No persistence or durable replay of any kind.
No queue, worker or async job model. No browser, JavaScript rendering, cookie or consent
handling. No PXK-62 privacy/security/third-party/legal contracts. No full `ArtifactDescriptor`.
No `ScoreModelDefinition`, `Scorecard`, overall score or `ProductDiagnosis`. No customer PDF or
renderer. No Google, competitor or AI search. **No authentication, no authorisation, no rate
limiting, no production deployment.** No site crawling, and no robots.txt or sitemap.xml
evaluation. No invasive probing of any kind.

PR #5 remains a frozen, unmerged reuse source at `ed9814b96a96cb57ad4460709c50fed599727794`.
It was read once to confirm it contains no HTTP, adapter or collector code, and was not
modified, branched from, or merged.

### Real-boundary smoke

Manual, and deliberately **not** part of the CI suite: no test in this repository touches the
network, and `python-compat.yml` has no isolation that would contain a flaky live call.

Command: `uv run python -m pxapi.adapters.inbound.cli <url>`

Re-run in full on the exact candidate `791cea527c36eb69d930a79e9a94fdd055bac5be` with a clean
working tree (`git status --porcelain` empty). The previous smoke belongs to the previous head
and is not evidence for this one.

**Required target — `https://example.com/`**, at `2026-09-07T14:11:53Z`:

| Metric | Assessment | Value | `source_url` |
| --- | --- | --- | --- |
| `HTTP_STATUS` | `KNOWN` | `200` | `https://example.com/` |
| `FINAL_URL` | `KNOWN` | `https://example.com/` | `https://example.com/` |
| `TRANSPORT_IS_HTTPS` | `KNOWN` | `true` | `https://example.com/` |
| `REDIRECT_COUNT` | `KNOWN` | `0` | `https://example.com/` |
| `CONTENT_TYPE` | `KNOWN` | `text/html` | `https://example.com/` |
| `PAGE_TITLE_PRESENT` | `KNOWN` | `true` | `https://example.com/` |
| `PAGE_TITLE` | `KNOWN` | `Example Domain` | `https://example.com/` |
| `META_DESCRIPTION_PRESENT` | `KNOWN` | `false` | `https://example.com/` |
| `META_DESCRIPTION` | `NOT_APPLICABLE` | — | `https://example.com/` |
| `CANONICAL_PRESENT` | `KNOWN` | `false` | `https://example.com/` |
| `CANONICAL_URL` | `NOT_APPLICABLE` | — | `https://example.com/` |

- run id `px-2e4e50e4b6cf47af83d69007ff71c80e`, run state `SUCCEEDED`,
  `entered_at` `2026-09-07T14:11:53Z`, `finished_at` `2026-09-07T14:11:54Z`
- collector `HOMEPAGE_BASELINE_COLLECTOR`, method version `1.0.0`
- 26 documents produced, **0 invalid** against the merged contracts
- 11 evidence documents, each referencing the `measurement_id` it was derived from; e.g.
  measurement `px-e3cf259beff841d28850571cfabe8906` -> evidence
  `px-abfc98fe2664456daeb45c16b6613f56`
- `evidence.source_url == referenced_measurement.source_url` for **11 of 11** references,
  checked directly against the emitted JSON rather than through the test suite
- no evidence document carries a polarity

This target does not redirect, so requested and final are the same URL and the provenance repair
is **not observable here** — which is the point of the exploratory target below.

**Optional exploratory target — `http://www.python.org/`** (not an acceptance gate and not a CI
dependency). It answers `301` to `https://www.python.org/`, which is the case the repair exists
for. 26 documents, 0 invalid, 11 of 11 evidence references matching:

| Metric | Value | `source_url` |
| --- | --- | --- |
| `FINAL_URL` | `https://www.python.org/` | `http://www.python.org/` |
| `REDIRECT_COUNT` | `1` | `http://www.python.org/` |
| `HTTP_STATUS` | `200` | `https://www.python.org/` |
| `TRANSPORT_IS_HTTPS` | `true` | `https://www.python.org/` |
| `CONTENT_TYPE` | `text/html; charset=utf-8` | `https://www.python.org/` |
| `PAGE_TITLE` | `Welcome to Python.org` | `https://www.python.org/` |
| `META_DESCRIPTION` | `The official home of the Python Programming Language` | `https://www.python.org/` |
| `CANONICAL_PRESENT` | `false` | `https://www.python.org/` |

Before the repair this run published `TRANSPORT_IS_HTTPS: true` with
`source_url: http://www.python.org/` — a record contradicting itself, and one no schema could
refuse. Both URL contexts now survive into the evidence documents as well.

**Blocked target, live:** `http://169.254.169.254/latest/meta-data/` produced run state
`FAILED`, `failure.code` `TARGET_NOT_PERMITTED`, every measurement
`not_assessed_reason: PERMISSION_DENIED`, **0** measurements carrying a `result` and **0**
evidence documents carrying a polarity. 25 documents, 0 invalid. No connection was attempted,
and the failure path's provenance is unchanged by this repair.

No secrets, tokens or raw response bodies were copied into this document.

## G. Blockers and follow-ups discovered

1. **The first smoke found a real defect, and it was the defect this slice exists to prevent.**
   python.org returns gzip whatever `Accept-Encoding` asks for. The parser received compressed
   bytes, found no `<title>`, and the run reported that the *site* had no title, description or
   canonical link. Fixed in `c205f81`: gzip and deflate are decoded, and an encoding we cannot
   decode is recorded as a runtime error of ours rather than as an absent element. This is the
   strongest argument in this document for keeping the live smoke a delivery requirement —
   1,092 passing offline tests did not catch it.
2. **A second truth defect was found by review, not by the suite: redirect provenance.**
   Every fact was published against the requested URL rather than the URL it was observed
   against. Fixed in `791cea5` and described in section D. Like the gzip defect, it survived a
   full green suite — 1,102 tests — because every offline test fetched a URL that does not
   redirect, so the two contexts were never distinguishable. The lesson is the same one section
   G.1 records: a passing suite proves the cases it exercises, and nothing else.
3. **Per-hop failure provenance was deliberately not redesigned.** Where no response was
   obtained there is no final URL, and every record keeps the requested URL. That is coherent,
   but a failure *after* one or more successful hops is not modelled at all: the chain is not
   represented, so a redirect that succeeded and then timed out reports only the original
   target. Whether the hop chain should be evidence is a product decision, not an
   implementation convenience, and is left open rather than settled quietly here.
4. **The reviewability figure is over the convention.** See section E; a Product Owner decision.
5. **Packaging.** `contracts/v1/` sits outside the wheel's `src/pxapi` package. The runtime
   locates it by path with a `PXAPI_CONTRACTS_DIR` override. Packaging the registry into a
   distributable artifact is deliberately deferred; deployment is out of scope.
6. **`brotli` and `zstd` responses are not decodable** and are reported as such. Whether to add
   a decoder is a product decision, not an implementation convenience.
7. **Evidence is currently one document per measurement.** That establishes the referencing
   chain honestly but adds no information over the measurements. Whether evidence should
   aggregate is a decision for the slice that first needs a judgement.

## H. Deliberately deferred

Whether a failed stage is fatal to a run stays orchestration policy, exactly as PXK-60 left it:
this slice fails the run when nothing could be fetched and succeeds it when the transport facts
were obtained, and no contract decides that for it. `not_assessed_reason` still carries no
severity and no retry policy.

## I. Status of this document

Written after the final measurement pass and re-read against the final tree state. Every value
was produced by the command printed beside it, in this repository, on this branch. The PR
number and head SHA are reported in the terminal evidence packet rather than here, because they
do not exist until this document is committed.
